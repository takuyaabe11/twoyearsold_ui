name: twoyearsold_ui
description: "TWOYEARSOLD shared design system & settings (One World). See ../DESIGN.md §10."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  shared_preferences: ^2.5.5
  audioplayers: ^6.1.0

flutter:
  uses-material-design: true
